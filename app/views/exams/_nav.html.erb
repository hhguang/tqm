<div>
<nav class="navbar navbar-default" role="navigation">
		  <!-- Brand and toggle get grouped for better mobile display -->
		  <div class="navbar-header">
	          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
	            <span class="icon-bar"></span>
	            <span class="icon-bar"></span>
	            <span class="icon-bar"></span>
	          </button>
	          <!-- <a class="navbar-brand" href="#">汕头市2012-2013学年度非毕业班教学质量监测</a> -->
	          <%= link_to @exam.name,exam_path(@exam),class: "navbar-brand" %>
	        </div>

		  <!-- Collect the nav links, forms, and other content for toggling -->
		  <div class="collapse navbar-collapse navbar-ex1-collapse">
		  	<p class=" text-danger navbar-text">状态：
		  		<% if can? :manage,@exam %>
		  		<%= link_to (@exam.closed? ? '已关闭' : '已启动'),start_exam_path(@exam),title: '点击切换状态' %>
		  		<% else %>
		  		<%= (@exam.closed? ? '已关闭' : '已启动') %>
		  		<% end %>

		  	</p>

		    <ul class="nav navbar-nav">		      
		      <li <%= "class=active" if ['exams'].include?(controller_name) && ['show'].include?(action_name) %> >
		      	<%= link_to "项目首页", exam_path(@exam) %>
		      </li>
		      <li <%= "class=active" if ['score_files'].include?(controller_name)  %> >
		      	<%= link_to "成绩文件", exam_score_files_path(@exam) %>
		      </li>
		      <li <%= "class=active" if ['reports'].include?(controller_name)  %> >
		      	<%= link_to "分析报告", exam_reports_path(@exam) %>
		      </li>
		    </ul>
		    
		    <ul class="nav navbar-nav navbar-right">
			<% if can? :manage, @exam %>		    	
		    	<li class="dropdown">
			        <a href="#" class="dropdown-toggle" data-toggle="dropdown">设置 <b class="caret"></b></a>
			        <ul class="dropdown-menu">
			          <li>		          	
			          	<%= link_to (@exam.closed? ? '启动监测项目' : '关闭监测项目'),start_exam_path(@exam) %>		          	
			          </li>
			          	<li>
			          	<%= link_to (@exam.paper_order.state? ? '启动试卷订单提交' : '关闭试卷订单提交'),order_on_off_exam_path(@exam) %>
			          	
				  		</li>
				  		<li>		          	
			          	<%= link_to (@exam.upload_started? ? '关闭成绩与分析文件上传' : '启动成绩与分析文件上传'),set_upload_started_exam_path(@exam) %>		          	
			          </li>
			        </ul>
			    </li>
		    <% end %>
		    <% if can? :create,@exam %>  
		      <li class="dropdown">
		        <a href="#" class="dropdown-toggle" data-toggle="dropdown">管理 <b class="caret"></b></a>
		        <ul class="dropdown-menu">
		          <li>
		          	<%= link_to '所有项目',exams_path %>		          	
		          </li>
		          <li role="presentation" class="divider">
		          <li>
		          	<%= link_to '试卷订单管理',exam_order_items_path(@exam) %>
		          </li>
		        </ul>
		      </li>
		    <% end %>
		    </ul>
		    
		  </div><!-- /.navbar-collapse -->
		</nav>
</div>